# New Features

- [ ] Move mock news data to a shared `lib/news.js` file so both the list page and single news page can import it
- [ ] Implement the single news page — look up the article by `newsSlug` from shared data and display full content + image
- [ ] Add a "← Back to News" link on the single news page
- [ ] Add category filter buttons on the news list page to show only articles matching a selected category
- [ ] Add a search input on the news list page to filter articles by title in real time
- [ ] Add a `not-found.jsx` page under `app/news/[newsSlug]/` for unknown slugs
