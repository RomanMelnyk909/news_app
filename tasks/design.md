# Design Improvements

- [ ] Add active link highlighting to MainHeader — use `usePathname()` from `next/navigation` to apply an active class to the current route link
- [ ] Make the featured news card responsive on mobile — stack image above content instead of side-by-side below ~640px
- [ ] Add a loading skeleton for news cards while data is being fetched
- [ ] Style the single news article page (`app/news/[newsSlug]/single-news.module.css`) — headline, image, body text, meta info
- [ ] Add a hover tooltip or transition effect on category badges
